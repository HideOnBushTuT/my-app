export default ({ totalCount, completedCount }) => {
    return (
        <div>
            <h1>Total Count: {totalCount}</h1>
            <h1>Completed Count: {completedCount}</h1>
        </div>
    )
}